services:
  api:
    environment:
      # Полный DSN — один источник правды:
      DATABASE_URL: "postgres://mm_user:muhammadjon@mm-postgres-prod:5432/mm_shop_prod?sslmode=disable"

      # На случай, если код собирает DSN из отдельных переменных:
      DB_HOST: "mm-postgres-prod"
      DB_PORT: "5432"
      DB_NAME: "mm_shop_prod"
      DB_USER: "mm_user"
      DB_PASSWORD: "muhammadjon"
      DB_SSL_MODE: "disable"

      # Остальной конфиг (по необходимости)
      GIN_MODE: "release"
      SERVER_HOST: "0.0.0.0"
      SERVER_PORT: "8080"
      PUBLIC_HOST: "159.89.99.252"

# Руководство по обновлению программ Libiss

## Обзор системы обновлений

В системе MM Shop реализованы **две отдельные системы** для управления файлами программ:

### 1. Система обновлений (Update System)
**Назначение:** Обновления для Flutter приложений (MM Shop клиент)
- **Платформы:** `server` (Node.js zip), `windows` (.exe), `android` (.apk)
- **Эндпоинт:** `/api/v1/admin/updates`
- **Публичный доступ:** `/api/v1/updates/latest?platform=windows|android|server`
- **Использование:** Для обновления мобильного/десктопного приложения MM Shop

### 2. Система Libiss POS (Libiss POS File Management)
**Назначение:** Управление файлами программ Libiss POS и Libiss Local API
- **Типы программ:**
  - `full` - Полный пакет (Касса1: программа + сервер + MySQL) - **~595 МБ**
  - `server_only` - Программа + сервер без MySQL - **~31.8 МБ**
  - `cassa2` - Программа для Касса2 - **~11.3 МБ**
- **Платформы:** `windows` (.exe), `android` (.apk)
- **Эндпоинт:** `/api/v1/admin/libiss-pos`
- **Публичный доступ:** `/api/v1/libiss-pos/public/:filename`
- **Использование:** Для распространения программ Libiss POS
- **Максимальный размер файла:** 500 МБ (достаточно для всех типов пакетов)

---

## Процесс обновления программ

### 📱 **Android APK (libiss_pos)**

#### Загрузка через админ-панель:
1. Войти в админ-панель (роль: `admin` или `super_admin`)
2. Перейти в раздел **"Libiss POS"**
3. Заполнить форму:
   - **Тип программы:** выбрать нужный тип (full/cassa2/server_only)
   - **Платформа:** выбрать `Android`
   - **Версия:** указать версию (например: `1.0.0`)
   - **Файл:** выбрать `.apk` файл
   - **Описание:** описание изменений (опционально)
   - **Публичный доступ:** отметить, если нужно публичное скачивание
4. Нажать **"Загрузить файл"**

#### Скачивание обновления:
**Для авторизованных пользователей:**
```
GET /api/v1/libiss-pos/download/:filename
Authorization: Bearer <token>
```

**Публичное скачивание (если включено):**
```
GET /api/v1/libiss-pos/public/:filename
```

**Получение последней версии:**
```
GET /api/v1/libiss-pos/latest?type=full&platform=android
```

#### Пример ответа:
```json
{
  "success": true,
  "data": {
    "id": "uuid",
    "type": "full",
    "platform": "android",
    "version": "1.0.0",
    "fileName": "full_1.0.0_abc12345.apk",
    "originalName": "libiss_pos_v1.0.0.apk",
    "fileUrl": "/api/v1/libiss-pos/download/full_1.0.0_abc12345.apk",
    "publicUrl": "/api/v1/libiss-pos/public/full_1.0.0_abc12345.apk",
    "fileSize": 15728640,
    "checksumSha256": "abc123...",
    "description": "Первая версия Android приложения",
    "isActive": true,
    "isPublic": true,
    "downloadCount": 0,
    "createdAt": "2024-01-01T12:00:00Z"
  }
}
```

---

### 💻 **Windows EXE (libiss_pos)**

#### Загрузка через админ-панель:
1. Войти в админ-панель
2. Перейти в раздел **"Libiss POS"**
3. Заполнить форму:
   - **Тип программы:** 
     - `full` - Полный пакет (~595 МБ): программа + сервер + MySQL
     - `server_only` - Программа + сервер (~31.8 МБ): без MySQL
     - `cassa2` - Только программа (~11.3 МБ)
   - **Платформа:** выбрать `Windows`
   - **Версия:** указать версию
   - **Файл:** выбрать `.exe` файл (максимум 600 МБ)
   - Остальные поля как для Android
4. Нажать **"Загрузить файл"**

#### Скачивание обновления:
Аналогично Android, но с `platform=windows`:
```
GET /api/v1/libiss-pos/latest?type=full&platform=windows
```

---

### 🖥️ **Libiss Local API (серверная часть)**

Libiss Local API может обновляться двумя способами:

#### Вариант 1: Через систему обновлений (Update System)
Если это Node.js сервер:
1. Перейти в раздел **"Обновления"** в админ-панели
2. Выбрать платформу: `server`
3. Загрузить `.zip` файл с сервером
4. Версия будет доступна через:
```
GET /api/v1/updates/latest?platform=server
```

#### Вариант 2: Через систему Libiss POS
Если это часть полного пакета (`full` или `server_only`):
1. Загрузить через раздел **"Libiss POS"**
2. Выбрать тип: `full` (с MySQL) или `server_only` (без MySQL)
3. Выбрать платформу: `windows` (если это Windows сервер)
4. Загрузить `.exe` файл

---

## API Эндпоинты для программ

### Админские эндпоинты (требуют авторизации admin/super_admin)

#### Список всех файлов:
```
GET /api/v1/admin/libiss-pos
GET /api/v1/admin/libiss-pos?type=full
GET /api/v1/admin/libiss-pos?platform=android
GET /api/v1/admin/libiss-pos?type=full&platform=windows
```

#### Загрузка файла:
```
POST /api/v1/admin/libiss-pos/upload
Content-Type: multipart/form-data

Form fields:
- type: full|cassa2|server_only
- platform: windows|android
- version: 1.0.0
- file: файл (.exe или .apk)
- description: описание (опционально)
- isPublic: true|false
```

#### Удаление файла:
```
DELETE /api/v1/admin/libiss-pos/:id
```

#### Информация о файле:
```
GET /api/v1/admin/libiss-pos/:id
```

---

### Публичные эндпоинты (без авторизации)

#### Получение последней версии:
```
GET /api/v1/libiss-pos/latest?type=full&platform=android
GET /api/v1/libiss-pos/latest?type=cassa2&platform=windows
```

#### Публичное скачивание:
```
GET /api/v1/libiss-pos/public/:filename
```

---

### Защищенные эндпоинты (требуют авторизации пользователя)

#### Скачивание файла:
```
GET /api/v1/libiss-pos/download/:filename
Authorization: Bearer <token>
```

---

## Структура файлов на сервере

```
/app/
├── libiss_pos/              # Файлы Libiss POS
│   ├── full/               # Полные пакеты (~595 МБ)
│   │   ├── full_1.0.0_xxx.exe  # Windows версия
│   │   └── full_1.0.0_xxx.apk  # Android версия
│   ├── cassa2/             # Программы для Касса2 (~11.3 МБ)
│   │   ├── cassa2_1.0.0_xxx.exe
│   │   └── cassa2_1.0.0_xxx.apk
│   └── server_only/        # Сервер без MySQL (~31.8 МБ)
│       ├── server_only_1.0.0_xxx.exe
│       └── server_only_1.0.0_xxx.apk
└── updates/                # Обновления Flutter приложений
    ├── server/             # Node.js сервер (zip)
    ├── windows/             # Windows клиент (exe)
    └── android/             # Android клиент (apk)
```

### Размеры файлов по типам:

| Тип программы | Размер | Описание |
|--------------|--------|----------|
| `full` | ~595 МБ | Полный пакет: libiss_pos + server + MySQL |
| `server_only` | ~31.8 МБ | libiss_pos + server (без MySQL) |
| `cassa2` | ~11.3 МБ | Только программа libiss_pos |

**Примечание:** Размеры указаны для Windows версий. Android версии могут отличаться по размеру.

---

## Примеры использования в клиентских приложениях

### Проверка обновления Libiss POS (Android):

```kotlin
// Kotlin пример
fun checkForUpdate() {
    val apiUrl = "https://api.libiss.com/api/v1/libiss-pos/latest"
    val request = Request.Builder()
        .url("$apiUrl?type=full&platform=android")
        .build()
    
    client.newCall(request).enqueue(object : Callback {
        override fun onResponse(call: Call, response: Response) {
            val data = JSONObject(response.body()?.string())
            val latestVersion = data.getJSONObject("data").getString("version")
            val currentVersion = getCurrentVersion()
            
            if (isNewerVersion(latestVersion, currentVersion)) {
                // Показать уведомление об обновлении
                val downloadUrl = data.getJSONObject("data").getString("publicUrl")
                downloadUpdate(downloadUrl)
            }
        }
    })
}
```

### Проверка обновления Libiss POS (Windows):

```csharp
// C# пример
public async Task<bool> CheckForUpdateAsync()
{
    var apiUrl = "https://api.libiss.com/api/v1/libiss-pos/latest";
    var request = $"{apiUrl}?type=full&platform=windows";
    
    using (var client = new HttpClient())
    {
        var response = await client.GetStringAsync(request);
        var data = JsonConvert.DeserializeObject<UpdateResponse>(response);
        
        var latestVersion = data.Data.Version;
        var currentVersion = GetCurrentVersion();
        
        if (IsNewerVersion(latestVersion, currentVersion))
        {
            // Скачать обновление
            await DownloadUpdateAsync(data.Data.PublicUrl);
            return true;
        }
    }
    return false;
}
```

---

## Безопасность

1. **Авторизация:** Загрузка файлов доступна только админам
2. **Проверка целостности:** Каждый файл имеет SHA256 checksum
3. **Публичный доступ:** Можно контролировать через флаг `isPublic`
4. **Версионирование:** Каждая версия хранится отдельно
5. **Счетчик скачиваний:** Отслеживается количество загрузок

---

## Рекомендации

1. **Версионирование:** Используйте семантическое версионирование (1.0.0, 1.1.0, 2.0.0)
2. **Описания:** Всегда заполняйте описание изменений
3. **Тестирование:** Тестируйте файлы перед загрузкой
4. **Резервные копии:** Храните старые версии для отката
5. **Уведомления:** Уведомляйте пользователей о критических обновлениях

---

## Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь, что файл загружен корректно
3. Проверьте права доступа к файлам
4. Убедитесь, что версия указана правильно


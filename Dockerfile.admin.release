FROM nginx:alpine

RUN apk add --no-cache curl

# Копируем конфиг Nginx
COPY admin.conf /etc/nginx/conf.d/default.conf

# Копируем файлы админки
COPY . /usr/share/nginx/html/

RUN mkdir -p /var/log/nginx && chown -R nginx:nginx /var/log/nginx /usr/share/nginx/html

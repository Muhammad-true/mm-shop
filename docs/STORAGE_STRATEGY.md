# Стратегия хранения файлов

## ✅ Реализованные правила

### 1. Bind Mounts для хранения файлов

Все файлы (изображения, обновления, Libiss POS) хранятся в bind mounts на хосте, а не в Docker-томах:

```yaml
volumes:
  - ./images:/app/images      # Изображения товаров
  - ./updates:/app/updates    # Файлы обновлений
  - ./libiss_pos:/app/libiss_pos  # Файлы Libiss POS
```

**Преимущества:**
- Простое резервное копирование (просто копируй папки)
- Легкий мониторинг размера диска
- Файлы сохраняются при пересоздании контейнеров
- Доступ к файлам напрямую с хоста

### 2. Автоматическая очистка старых обновлений

Скрипт `scripts/cleanup_old_updates.sh` удаляет файлы обновлений старше указанного количества дней (по умолчанию 10 дней).

**Использование:**

```bash
# Вручную
./scripts/cleanup_old_updates.sh [дней] [путь_к_папке]

# Пример: удалить файлы старше 7 дней
./scripts/cleanup_old_updates.sh 7 ./updates
```

**Автоматическая очистка через systemd timer:**

```bash
# Настройка (один раз)
sudo ./scripts/setup_cleanup_timer.sh

# Очистка будет выполняться каждый день в 3:00 ночи
```

**Полезные команды:**
- Статус таймера: `sudo systemctl status cleanup-old-updates.timer`
- Запустить вручную: `sudo systemctl start cleanup-old-updates`
- Логи: `sudo journalctl -u cleanup-old-updates -f`

### 3. Сжатие изображений "на лету"

При загрузке изображений они автоматически сжимаются:

- **JPEG**: качество 85% (хороший баланс между размером и качеством)
- **PNG**: конвертируется в JPEG для лучшего сжатия
- **WebP**: конвертируется в JPEG для лучшего сжатия
- **GIF**: сохраняется без изменений

**Результат:**
- Уменьшение размера файлов на 60-80% без видимой потери качества
- Экономия места на диске
- Быстрее загрузка для пользователей

**Логирование:**
При загрузке изображения в логах отображается:
```
✅ Файл успешно сохранен: 245760 байт записано (было 1024000 байт, сжато на 76.0%, сэкономлено 778240 байт)
```

## 📋 Рекомендации

### Резервное копирование

Регулярно делай бэкап папок:
- `./images/` - все изображения товаров
- `./updates/` - файлы обновлений (можно исключить старые версии)
- `./libiss_pos/` - файлы Libiss POS

### Мониторинг размера

Проверяй размер папок:
```bash
du -sh ./images ./updates ./libiss_pos
```

### Настройка качества сжатия

Если нужно изменить качество JPEG, отредактируй `controllers/upload_controller.go`:
```go
quality := 85  // Измени на нужное значение (1-100)
```

- 90-100: высокое качество, большой размер
- 80-90: хороший баланс (рекомендуется)
- 70-80: среднее качество, хорошее сжатие
- <70: низкое качество, максимальное сжатие


# 🚀 Быстрый старт: Настройка Cloudinary

## На Cloudinary (5 минут)

1. **Зарегистрируйтесь:** https://cloudinary.com/users/register/free

2. **Получите данные из Dashboard:**
   - Откройте: https://cloudinary.com/console
   - Скопируйте:
     - **Cloud name** (например: `driokajen`)
     - **API Key**
     - **API Secret**

3. **Создайте Upload Preset:**
   - Settings → Upload → Add upload preset
   - **Название:** `mm-shop-products`
   - **Режим подписи:** выберите **"Без подписи"** (Unsigned) ⚠️ ВАЖНО!
   - **Папка ресурсов:** `variations`
   - **Overwrite:** `false` (не перезаписывать существующие файлы)
   - **Use filename:** `false` (не использовать имя файла как public_id)
   - **Unique filename:** `true` ✅ (генерировать уникальное имя)
   - **Disallow public id:** `true` ✅ (не разрешать указывать public_id вручную)
   - **Use filename as display name:** `true` ✅ (использовать имя файла как отображаемое имя)
   - **Use asset folder as public id prefix:** `false` (не использовать папку как префикс)
   - **Type:** `upload` (тип загрузки)
   - **Asset folder:** `variations` (папка для ресурсов)
   - **Трансформации:** (вкладка "Incoming Transformation")
     
     ⚠️ **ВАЖНО:** `fl_auto` НЕЛЬЗЯ использовать в Upload Preset! 
     Флаг `auto` для обработки EXIF ориентации передается через API параметр `flags: "auto"` (уже настроено в коде).
     
     **Вариант 1: Простая трансформация (без удаления фона):**
     ```
     c_fill,g_auto,h_1200,w_1200,b_white
     ```
     
     **Вариант 2: С удалением фона (платная функция Cloudinary):**
     ```
     c_fill,g_auto,h_1200,w_1200/b_rgb:D5FFD1/c_auto,h_1200,w_1200/f_jpg/e_background_removal:fineedges_y/c_auto_pad,g_auto,h_1200,w_1200
     ```
     
     **Расшифровка сложной цепочки:**
     - `c_fill,g_auto,h_1200,w_1200` - заполнение с авто-гравитацией
     - `/b_rgb:D5FFD1` - фон светло-зеленый (или `b_white` для белого)
     - `/c_auto,h_1200,w_1200` - авто-кадрирование
     - `/f_jpg` - формат JPG
     - `/e_background_removal:fineedges_y` - удаление фона с тонкими краями (платная функция)
     - `/c_auto_pad,g_auto,h_1200,w_1200` - авто-падинг с гравитацией
     
     **Про удаление фона:**
     - `e_background_removal:fineedges_y` - платная функция (~$0.02-0.05 за изображение)
     - На бесплатном тарифе: 25 ГБ хранилища, 25 тыс. трансформаций/месяц
     - Без этого параметра фон удаляться НЕ будет
     - Можно использовать без удаления фона - просто уберите эту часть из цепочки
   
   - **Eager трансформации:** (опционально)
     ```
     f_auto/q_auto
     ```
     - `f_auto` - автоматический формат (WebP для поддерживающих браузеров)
     - `q_auto` - автоматическое качество
   - **Формат:** `jpg` (вкладка "Upload Manipulations")
   
4. **Настройте "Optimize and Deliver":**
   - **Delivery type:** `Upload` (по умолчанию)
   - **Access control:** оставьте пустым (или "Select..." без выбора)
   - **Format:** `jpg` (или оставьте пустым для автоматического)
   - **Allowed formats:** оставьте пустым (разрешить все форматы)
   - **Discard original file name when delivering assets as attachments:** оставьте выключенным
   
5. **Сохраните:**
   - Нажмите **Save** внизу страницы

## На сервере (2 минуты)

1. **Отредактируйте `.env.production`:**
   ```bash
   cd ~/mm-shop/release
   nano .env.production
   ```

2. **Добавьте переменные:**
   ```bash
   USE_CLOUDINARY=true
   CLOUDINARY_CLOUD_NAME=ваш-cloud-name
   CLOUDINARY_API_KEY=ваш-api-key
   CLOUDINARY_API_SECRET=ваш-api-secret
   CLOUDINARY_UPLOAD_PRESET=mm-shop-products
   CLOUDINARY_REMOVE_BACKGROUND=false
   ```

3. **Перезапустите API:**
   ```bash
   docker compose -f docker-compose.release.yml restart api
   ```

4. **Проверьте логи:**
   ```bash
   docker compose -f docker-compose.release.yml logs -f api
   ```

5. **Загрузите тестовое фото:**
   - Через админ-панель загрузите фото товара
   - В логах должно быть: `☁️ Обработка изображения товара через Cloudinary...`
   - В ответе будет URL от Cloudinary

## ✅ Готово!

Теперь все фото товаров будут:
- ✅ Правильно поворачиваться (EXIF ориентация)
- ✅ Автоматически обрабатываться
- ✅ Храниться в Cloudinary с CDN

## 📖 Подробная инструкция

См. [CLOUDINARY_SETUP.md](./CLOUDINARY_SETUP.md)


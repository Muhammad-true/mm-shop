# Проверка эндпоинтов API для удаления аккаунта

## ✅ КРАТКОЕ РЕЗЮМЕ

**Статус:** ✅ **Эндпоинт СОЗДАН**

- ✅ Эндпоинт `DELETE /api/v1/users/delete-account` **создан** в коде бэкенда
- ✅ Метод `DeleteAccount` добавлен в `auth_controller.go`
- ✅ Роут добавлен в `routes.go` в группу `protected -> users`
- ✅ Реализовано мягкое удаление (деактивация аккаунта)

---

## ✅ РЕЗУЛЬТАТЫ ПРОВЕРКИ КОДА БЭКЕНДА

### 📋 Текущие эндпоинты в проекте

### Используемые эндпоинты авторизации:

1. **Вход:** `POST /api/v1/auth/login` ✅ (найден в routes.go:61)
2. **Регистрация:** `POST /api/v1/auth/register` ✅ (найден в routes.go:60)
3. **Обновление токена:** `POST /api/v1/auth/refresh` ✅ (найден в routes.go:62)
4. **Профиль пользователя:** `GET /api/v1/users/profile` ✅ (найден в routes.go:154) ⚠️ **НЕ `/api/v1/auth/me`**
5. **Восстановление пароля:** `POST /api/v1/auth/forgot-password` ✅ (найден в routes.go:64)
6. **Выход:** `POST /auth/logout` ❌ **НЕ НАЙДЕН** (закомментирован в routes.go:65)
7. **Удаление аккаунта:** `DELETE /api/v1/auth/delete-account` ❌ **НЕ НАЙДЕН**

## ✅ РЕАЛИЗАЦИЯ ЗАВЕРШЕНА

**Эндпоинт для удаления аккаунта СОЗДАН!**

### Что реализовано:
- ✅ Метод `DeleteAccount` в `auth_controller.go` - удаление собственного аккаунта
- ✅ Роут `DELETE /api/v1/users/delete-account` добавлен в `routes.go`
- ✅ Реализовано мягкое удаление (деактивация через `IsActive = false`)

### Эндпоинты для удаления:
- `DELETE /api/v1/users/delete-account` - удаление собственного аккаунта (требует авторизации)
- `DELETE /api/v1/admin/users/:id` - удаление другого пользователя (только для админов)

## ❓ Вопросы для проверки на сервере

### 1. Эндпоинт для удаления аккаунта

**✅ Реализованный вариант:**
```
DELETE /api/v1/users/delete-account
```

**Детали:**
- ✅ Путь: `/api/v1/users/delete-account`
- ✅ Метод: `DELETE`
- ✅ Требует авторизации: Да (Bearer token)
- ✅ Удаляет: Собственный аккаунт текущего пользователя
- ✅ Тип удаления: Мягкое (деактивация через `IsActive = false`)

### 2. Метод HTTP запроса

**Текущий:** `DELETE`

**Альтернативы:**
- `POST /api/v1/auth/delete-account`
- `PUT /api/v1/auth/delete-account`

### 3. Требуемые заголовки

**Текущие заголовки:**
```json
{
  "Authorization": "Bearer {token}",
  "Content-Type": "application/json"
}
```

**Нужно проверить:**
- Требуется ли токен авторизации? ✅ (да, используется)
- Нужны ли дополнительные заголовки?

### 4. Тело запроса

**✅ Реализация:** Тело запроса не требуется (только заголовки)

**Текущая реализация:**
- Тело запроса не отправляется
- Достаточно только заголовка `Authorization: Bearer <token>`
- Пользователь удаляется автоматически на основе токена

**Примечание:** В будущем можно добавить подтверждение пароля для дополнительной безопасности:
```json
{
  "password": "user_password"
}
```

### 5. Формат ответа

**✅ Реализованный формат:**
```json
{
  "success": true,
  "message": "Account deleted successfully",
  "data": null
}
```

**Коды статуса:**
- `200 OK` - аккаунт успешно удален
- `401 Unauthorized` - токен отсутствует или невалиден
- `400 Bad Request` - аккаунт уже удален (деактивирован)
- `500 Internal Server Error` - ошибка сервера при удалении

## 🔍 Как проверить

### Вариант 1: Проверить документацию API

Если есть Swagger/OpenAPI документация:
- Откройте документацию API
- Найдите эндпоинт для удаления аккаунта
- Проверьте метод, путь и параметры

### Вариант 2: Проверить код бэкенда

Если есть доступ к коду сервера:
- Найдите роутер/контроллер для авторизации
- Проверьте маршрут для удаления аккаунта

### Вариант 3: Тестовый запрос

Можно сделать тестовый запрос через Postman или curl:

```bash
# ✅ Правильный запрос (реализованный эндпоинт)
curl -X DELETE https://api.libiss.com/api/v1/users/delete-account \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json"

# Для локальной разработки:
curl -X DELETE http://localhost:8080/api/v1/users/delete-account \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json"
```

## 📝 Текущая реализация в коде

**Файл:** `lib/data/repositories/auth_repository.dart`

```dart
// Удаление аккаунта
Future<ApiResponse<Map<String, dynamic>>> deleteAccount(String token) async {
  final response = await ApiService.client.delete(
    Uri.parse('$_baseUrl/api/v1/auth/delete-account'),
    headers: ApiService.getAuthHeaders(token),
  );

  return ApiService.handleResponse(
    response,
    (json) => ApiResponse.fromJson(json, (j) => j),
    errorMessage: 'Ошибка удаления аккаунта',
  );
}
```

## ✅ Реализация завершена

### Эндпоинт создан и готов к использованию!

**Детали реализации:**
- ✅ Путь: `DELETE /api/v1/users/delete-account`
- ✅ Метод: `DELETE`
- ✅ Авторизация: Требуется (Bearer token)
- ✅ Логика: Мягкое удаление (деактивация аккаунта)
- ✅ Файлы изменены:
  - `controllers/auth_controller.go` - добавлен метод `DeleteAccount`
  - `routes/routes.go` - добавлен роут в группу `users`

**Тестирование:**
1. Можно протестировать через Postman или curl
2. Убедиться, что токен валиден
3. Проверить, что аккаунт деактивируется после запроса

## ✅ Реализованный код

### Роут в routes.go:
```go
// В группе protected -> users:
users.DELETE("/delete-account", authController.DeleteAccount)
```

**Путь:** `DELETE /api/v1/users/delete-account`

### Метод в auth_controller.go:
```go
// DeleteAccount удаляет аккаунт текущего пользователя
func (ac *AuthController) DeleteAccount(c *gin.Context) {
    user, exists := c.Get("user")
    if !exists {
        c.JSON(http.StatusUnauthorized, models.ErrorResponseWithCode(
            models.ErrAuthRequired,
            "User not found",
        ))
        return
    }

    userModel := user.(models.User)

    // Проверяем, что пользователь активен
    if !userModel.IsActive {
        c.JSON(http.StatusBadRequest, models.ErrorResponseWithCode(
            models.ErrValidationError,
            "Account is already deleted",
        ))
        return
    }

    // Мягкое удаление - деактивируем аккаунт
    userModel.IsActive = false
    if err := database.DB.Save(&userModel).Error; err != nil {
        log.Printf("❌ Ошибка удаления аккаунта пользователя %s: %v", userModel.ID, err)
        c.JSON(http.StatusInternalServerError, models.ErrorResponseWithCode(
            models.ErrInternalError,
            "Failed to delete account",
        ))
        return
    }

    log.Printf("✅ Аккаунт пользователя %s успешно удален (деактивирован)", userModel.ID)

    c.JSON(http.StatusOK, models.SuccessResponse(
        nil,
        "Account deleted successfully",
    ))
}
```

### Обновление Flutter кода:

**✅ Правильный путь для Flutter:**
```dart
// В auth_repository.dart:
Future<ApiResponse<Map<String, dynamic>>> deleteAccount(String token) async {
  final response = await ApiService.client.delete(
    Uri.parse('$_baseUrl/api/v1/users/delete-account'), // ✅ Обновлен путь
    headers: ApiService.getAuthHeaders(token),
  );

  return ApiService.handleResponse(
    response,
    (json) => ApiResponse.fromJson(json, (j) => j),
    errorMessage: 'Ошибка удаления аккаунта',
  );
}
```

**Изменения:**
- ✅ Путь: `/api/v1/users/delete-account` (вместо `/api/v1/auth/delete-account`)
- ✅ Метод: `DELETE` (без изменений)
- ✅ Тело запроса: не требуется (без изменений)

## 📞 Контакты для уточнения

Если нужно связаться с бэкенд-разработчиком:
- Email: sodiqov.online@gmail.com
- Telegram: @MuhammadTrue
- Телефон: +992 927 781 020


# Настройка Cloudinary для обработки изображений

## 📋 Обзор

Cloudinary - это облачный сервис для обработки изображений, который автоматически:
- ✅ Обрабатывает EXIF ориентацию (правильно поворачивает фото с телефонов)
- ✅ Удаляет фон (опционально, платная функция)
- ✅ Изменяет размер и сжимает изображения
- ✅ Предоставляет CDN для быстрой загрузки

## 🚀 Настройка на Cloudinary

### Шаг 1: Регистрация и получение учетных данных

1. **Зарегистрируйтесь на Cloudinary:**
   - Перейдите на https://cloudinary.com/users/register/free
   - Создайте бесплатный аккаунт (25GB хранилища, 25GB трафика/месяц)

2. **Получите учетные данные:**
   - После регистрации откройте Dashboard: https://cloudinary.com/console
   - В разделе **Account Details** найдите:
     - **Cloud name** (например: `driokajen`)
     - **API Key**
     - **API Secret**

### Шаг 2: Создание Upload Preset

1. **Перейдите в Settings → Upload:**
   - В левом меню выберите **Settings** → **Upload**

2. **Создайте Upload Preset:**
   - Нажмите **Add upload preset**
   - **Название:** `mm-shop-products` (или любое другое)
   - **Режим подписи:** выберите **"Без подписи"** (Unsigned) ⚠️ ВАЖНО!
     - Это позволяет загружать изображения без подписи API
   - **Папка ресурсов:** `variations` (или `products`)
   - **Замените ресурсы с тем же публичным идентификатором:** ✅ включите
   - **Сгенерированный публичный идентификатор:**
     - ✅ **Автоматически сгенерировать непонятное значение** (рекомендуется)
     - Или: ✅ **Используйте имя загруженного файла** + ✅ **Добавить уникальный суффикс**
   - **Сгенерированное отображаемое имя:**
     - ✅ **Используйте имя загруженного файла**
   - **Трансформации:** (вкладка "Incoming Transformation" или "Upload Manipulations")
     ```
     w_1200,h_1200,c_fit,b_white,q_auto:good,fl_auto
     ```
     Где:
     - `w_1200` - максимальная ширина 1200px
     - `h_1200` - максимальная высота 1200px
     - `c_fit` - сохраняет пропорции, вписывает в рамку
     - `b_white` - белый фон
     - `q_auto:good` - автоматическое качество
     - `fl_auto` - **автоматическая обработка EXIF ориентации** (важно!)
   - **Формат:** `jpg` (вкладка "Upload Manipulations")
   - Нажмите **Save**

3. **Скопируйте имя preset:**
   - Запомните имя preset (например: `mm-shop-products`)

### Шаг 3: Настройка на сервере

#### Вариант A: Через переменные окружения (рекомендуется)

Создайте или отредактируйте файл `.env.production` на сервере:

```bash
# Cloudinary Configuration
USE_CLOUDINARY=true
CLOUDINARY_CLOUD_NAME=ваш-cloud-name
CLOUDINARY_API_KEY=ваш-api-key
CLOUDINARY_API_SECRET=ваш-api-secret
CLOUDINARY_UPLOAD_PRESET=mm-shop-products
CLOUDINARY_REMOVE_BACKGROUND=false  # true для удаления фона (платная функция)
```

#### Вариант B: Через docker-compose.release.yml

Добавьте переменные окружения в `docker-compose.release.yml`:

```yaml
api:
  environment:
    - USE_CLOUDINARY=true
    - CLOUDINARY_CLOUD_NAME=ваш-cloud-name
    - CLOUDINARY_API_KEY=ваш-api-key
    - CLOUDINARY_API_SECRET=ваш-api-secret
    - CLOUDINARY_UPLOAD_PRESET=mm-shop-products
    - CLOUDINARY_REMOVE_BACKGROUND=false
```

### Шаг 4: Перезапуск сервиса

```bash
cd ~/mm-shop/release
docker compose -f docker-compose.release.yml restart api
```

Или полный перезапуск:

```bash
docker compose -f docker-compose.release.yml down
docker compose -f docker-compose.release.yml up -d
```

## 🔍 Проверка работы

1. **Проверьте логи:**
   ```bash
   docker compose -f docker-compose.release.yml logs -f api
   ```

2. **Загрузите тестовое изображение:**
   - Через админ-панель загрузите фото товара
   - В логах должно быть: `☁️ Обработка изображения товара через Cloudinary...`
   - В ответе должен быть `provider: "cloudinary"` и URL от Cloudinary

3. **Проверьте результат:**
   - Откройте URL изображения в браузере
   - Изображение должно быть правильно повернуто
   - Размер должен быть 1200x1200 или меньше (с сохранением пропорций)
   - Фон должен быть белым

## 🎨 Трансформации изображений

Cloudinary автоматически применяет следующие трансформации:

- `w_1200` - максимальная ширина 1200px
- `h_1200` - максимальная высота 1200px
- `c_fit` - сохраняет пропорции, вписывает в рамку
- `b_white` - белый фон
- `q_auto:good` - автоматическое качество (хороший баланс)
- `fl_auto` - **автоматическая обработка EXIF ориентации** (важно!)

### Удаление фона (платная функция)

Если хотите использовать удаление фона, установите:

```bash
CLOUDINARY_REMOVE_BACKGROUND=true
```

И добавьте `e_background_removal` в трансформации (уже включено в код).

**Стоимость:** ~$0.02-0.05 за изображение (зависит от размера)

## 💰 Тарифы Cloudinary

### Бесплатный тариф (Free):
- ✅ 25GB хранилища
- ✅ 25GB трафика/месяц
- ✅ Базовые трансформации
- ✅ CDN
- ❌ Удаление фона (платно)

### Платные тарифы:
- **Plus:** $99/месяц - 100GB хранилища, 100GB трафика
- **Advanced:** $224/месяц - 500GB хранилища, 500GB трафика

## 🔄 Переключение между локальной и Cloudinary обработкой

Чтобы переключиться обратно на локальную обработку:

```bash
USE_CLOUDINARY=false
```

Или просто удалите/закомментируйте переменные Cloudinary.

## 📊 Мониторинг использования

1. **В Dashboard Cloudinary:**
   - Перейдите в **Media Library** для просмотра загруженных изображений
   - В **Analytics** можно посмотреть статистику использования

2. **Проверка лимитов:**
   - В **Settings → Usage** можно посмотреть текущее использование
   - Бесплатный тариф: 25GB хранилища, 25GB трафика/месяц

## 🛠️ Устранение проблем

### Проблема: "Invalid API Key"

**Решение:**
- Проверьте правильность `CLOUDINARY_API_KEY` и `CLOUDINARY_API_SECRET`
- Убедитесь, что нет лишних пробелов

### Проблема: "Upload preset not found"

**Решение:**
- Проверьте имя preset в `CLOUDINARY_UPLOAD_PRESET`
- Убедитесь, что preset создан и имеет режим **Unsigned**

### Проблема: Изображения все еще поворачиваются неправильно

**Решение:**
- Убедитесь, что в трансформациях есть `fl_auto`
- Проверьте, что используется Cloudinary (в логах должно быть `☁️ Обработка...`)

### Проблема: Ошибка "Insufficient credits"

**Решение:**
- Проверьте лимиты в Dashboard
- Удаление фона требует платных кредитов
- Отключите `CLOUDINARY_REMOVE_BACKGROUND=false` если не нужен

## 📝 Пример ответа API

При успешной загрузке в Cloudinary:

```json
{
  "url": "https://res.cloudinary.com/driokajen/image/upload/v1234567890/variations/abc123.jpg",
  "public_id": "variations/abc123",
  "width": 1200,
  "height": 800,
  "format": "jpg",
  "bytes": 245678,
  "provider": "cloudinary"
}
```

При локальной обработке:

```json
{
  "success": true,
  "url": "/images/variations/abc123.jpg",
  "filename": "abc123.jpg",
  "size": 245678,
  "folder": "variations"
}
```

## 🔐 Безопасность

⚠️ **ВАЖНО:** Не коммитьте API Secret в Git!

- Используйте переменные окружения
- Храните секреты в `.env.production` (добавьте в `.gitignore`)
- Используйте Upload Preset с режимом **Unsigned** для безопасности

## 📚 Дополнительные ресурсы

- [Cloudinary Documentation](https://cloudinary.com/documentation)
- [Image Transformations](https://cloudinary.com/documentation/image_transformations)
- [Upload Presets](https://cloudinary.com/documentation/upload_presets)
- [Background Removal](https://cloudinary.com/documentation/ai_background_removal_addon)

